{% extends 'base.html.twig' %}

{% block title %}NAME{% endblock %}

{% block body %}
<div class="row" style="padding-top:10rem">
        <div class="col-md-5">
            <img src="{{ data.results.0.image }}" alt="{{ data.results.0.name}}" class="img-fluid"></img>        
        </div>
        <div class="col-md-7 my-auto">
            <h2>{{ data.results.0.name}}</h2>
                <ul style="	list-style: none;">
                    <h5>
                        <li>Espèce: {{ data.results.0.species }}</li>       
                        <li>{{ data.results.0.gender }}</li>         
                    </h5>
                </ul>
        <hr>     
        </div>
    </div>
{% endblock %}
